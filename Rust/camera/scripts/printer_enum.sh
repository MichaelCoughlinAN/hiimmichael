#!/bin/bash

# Generic printer enumeration script
# This script enumerates available printers and returns them as JSON

# Example output format expected by the application
echo '[{"name": "Default Printer", "status": "online"}, {"name": "Backup Printer", "status": "offline"}]'