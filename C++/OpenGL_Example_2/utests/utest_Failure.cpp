#include <cstdlib>

int main(int argc, char *argv[])
{
  // This test will always fail, but it illustrates the use of
  // returning EXIT_FAILURE. If the code were to be successful, you
  // can use EXIT_SUCCESS instead. Feel free to try the swap and then
  // re-run make test.
  exit(EXIT_FAILURE);
}
