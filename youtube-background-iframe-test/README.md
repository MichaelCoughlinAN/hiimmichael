## Local Development Notes

⚠️ **Important:**  
This project must be run using a local HTTP server. Opening `index.html`
directly in the browser (`file://`) will prevent the YouTube background
from loading due to browser security restrictions.

### How to run locally

```bash
python -m http.server
````

Then open:

```
http://localhost:8000
```

